# mixOmics vignette files

All Rproject files are storned in `./vignetteRproject`. 

`./BiocStyle`:    Contains the vignette files in Bioconductor format.
`./pdf_book` :    Contains the vignette files in pdf          format.
`./ (top-level)`: Contains the vignette files in html format.

## Re-render the vignettes

Open the .Rproj file and in `vignetteRproject/_bookdown.yml`, keep the relevant `output_dir` line. Note that for `html` output we need all the files in the top-level directory of the branch for `GitHub` to recognise them.

